//let dodoList = $('ul');
/* var options = {weekday: 'long', day: 'numeric', month: 'long', year: 'numeric'}
var today = new Date();
let weekDay = today.toLocaleDateString("en-US", {weekday: 'long'});


$('h1').html(weekDay + "'s ToDo List"); */
//dodoList.append('<li>something-2</li>');
/* $('li').function() {
    if($('this.input.checkbox_check').is(':checked')) {
        this.h2.addClass("done");
    }
}
 */